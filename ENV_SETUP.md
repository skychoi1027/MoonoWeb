# 환경 변수 설정 가이드

이 가이드에서는 MoonoWeb 프로젝트를 실행하기 위해 필요한 환경 변수 파일을 설정하는 방법을 설명합니다.

## 📋 설정 방법

### 클라이언트 환경 변수 설정

1. `client` 디렉토리로 이동합니다:
   ```bash
   cd client
   ```

2. `.env.example` 파일을 `.env`로 복사합니다:
   ```bash
   # Windows (PowerShell)
   Copy-Item .env.example .env
   
   # Windows (CMD)
   copy .env.example .env
   
   # Mac/Linux
   cp .env.example .env
   ```

3. `.env` 파일을 열고 백엔드 서버 URL을 설정합니다:
   - 개발 환경: `http://localhost:5000`
   - 프로덕션 환경: 실제 배포된 서버 URL

## 🔑 필수 환경 변수

### 클라이언트 (client/.env)

| 변수명 | 설명 | 필수 여부 | 예시 |
|--------|------|----------|------|
| `REACT_APP_API_URL` | 백엔드 API 서버 URL | ✅ 필수 | `http://localhost:5000` |


## ⚠️ 주의사항

- `.env` 파일은 절대 Git에 커밋하지 마세요!
- `.env` 파일에는 실제 API 키와 비밀번호가 포함되어 있습니다
- `.env.example` 파일은 템플릿으로만 사용하세요
- 프로덕션 환경에서는 환경 변수를 안전하게 관리하세요

## 🚀 빠른 시작

```bash
# 클라이언트 환경 변수 설정
cd client
cp .env.example .env
# .env 파일을 열어서 REACT_APP_API_URL 확인 (기본값 사용 가능)
```

## 📚 추가 정보

더 자세한 내용은 [README.md](./README.md)를 참고하세요.

